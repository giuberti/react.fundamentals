import React from 'react';

// eslint-disable-next-line
export default (props) => {
    return (
        <div>
            <h1>{props.titulo}</h1>
            <h2>{props.subTitulo}</h2>
        </div>
    )
}